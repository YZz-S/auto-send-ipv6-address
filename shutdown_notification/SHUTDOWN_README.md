# 智能关机通知工具

这个工具解决了Windows系统关机时无法可靠发送邮件通知的问题。

## 问题分析

原始的`shutdown_notification.py`脚本依赖于Windows的关机事件触发机制，但由于关机过程中网络服务可能会在邮件发送完成前被终止，导致通知邮件发送失败。

## 解决方案

新的解决方案通过**反转操作顺序**来解决这个问题：

1. 先发送关机通知邮件
2. 等待邮件发送完成
3. 然后启动系统关机过程

这样可以确保在网络服务终止前，通知邮件已经成功发送。

## 使用方法

### 方法1：使用智能关机工具（推荐）

1. 双击运行`smart_shutdown.bat`
2. 选择需要的操作（关机、重启或注销）
3. 设置延迟时间（默认60秒）
4. 确认后，工具会:
   - 先发送通知邮件
   - 然后启动系统关机/重启/注销过程

### 方法2：命令行使用

```
python shutdown_helper.py --type shutdown --delay 60
```

参数说明:
- `--type`: 操作类型，可选值: `shutdown`(关机), `restart`(重启), `logoff`(注销)
- `--delay`: 延迟时间（秒）

### 创建桌面快捷方式

运行`create_shortcut.bat`可在桌面创建智能关机工具的快捷方式。

## 文件说明

- `shutdown_helper.py` - 主要的智能关机脚本
- `shutdown_notification.py` - 发送邮件通知的脚本（被智能关机脚本调用）
- `smart_shutdown.bat` - 用户友好的界面脚本
- `create_shortcut.bat` - 创建桌面快捷方式的脚本
- `shutdown_helper.log` - 智能关机过程的日志文件

## 日志

脚本会记录详细的操作日志在`shutdown_helper.log`文件中，可用于排查问题。

## 注意事项

1. 请确保已正确配置`config.json`中的邮箱设置
2. 此工具需要Python环境才能运行
3. 使用智能关机工具替代系统原生的关机按钮，以确保通知能够发送 